These files are just the base files created to run our baseline code.

TreeTagger nor is the HeidelTime files included, see references to location of these files.

This is moreso used as a reference more than anything else.
