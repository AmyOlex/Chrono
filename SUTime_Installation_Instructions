Here are the instructions I have to get SUTime working, I am using Linux Mint 18.2 Cinnamon, Java: 1.8.0_131, Python: 3.5.2

I did this on a clean install of linux on my virtual box...

1. Open terminal
2. Navigate to Home Directory (or wherever you want this to be installed)
3. I create a python enviroment for any development I do, not sure if this is required but I installed the following
  a. sudo apt-get install -y-python3-venv
  b. (from home directory) mkdir environments
  c. cd enviroments
  d. python3 -m venv my_env
  e. source my_env/bin/activate
4. Run the following commands
  a. sudo apt-get install -y python3-pip
  b. sudo apt-get install python3-jpype #install jpype
  c. sudo apt-get instll python3-dev	#install python development enviroment
  d. pip install sutime #install sutime 
 5. I am still working on the best way to get the jar files installed...
  a. For now download them from the google drive (the models jar is a seperate download due to it's size)
  b. If your installation is the same as mine there should be a directory:
    i. ...environments/my_env/lib/python3.5/site-packages/sutime/jars
    ii. Copy all of the jar files to this directory (I have 26 in total)
 6. I created (copied and tweaked sutime gits test.py) the following file to test out the installation
    '''python code''' 
    import os
    import json
    from sutime import SUTime
    
    if__name__ == '__main__':
      test_case = u'I needed a desk tomorrow from 2pm to 3pm;
      jar_files = '..environments/my_env/lib/python3.5/site-packages/sutime/jars'
      sutime=SUTime(jars=jar_files,mark_time_ranges=True)
      print(json.dumps(sutime.parse(test_case),sort_keys=True,indent=4))
      
      '''End Python code'''
